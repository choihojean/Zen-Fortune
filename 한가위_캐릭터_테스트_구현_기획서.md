# 주니어보드 추석 이벤트 구현 기획서
## 프로젝트명: 나만의 한가위 캐릭터 테스트 + 럭키드로우

> 본 문서는 AI 코딩 에이전트 또는 개발 담당자가 바로 구현에 착수할 수 있도록 작성한 기능 중심 기획서이다.  
> **비주얼 디자인, 컬러, 폰트, 일러스트 스타일 등 UI 디자인은 별도 작업**으로 진행하며, 이 문서에서는 화면 구조·기능·데이터·로직·예외처리에 집중한다.

---

## 1. 프로젝트 개요

### 1.1 목적
추석을 맞아 임직원이 점심시간 등 짧은 시간 안에 가볍게 참여할 수 있는 **무인형 사내 이벤트 웹페이지**를 만든다.

참여자는 여러 개의 객관식 상황형 질문에 답하고, 답변 조합에 따라 자신에게 맞는 **한가위 캐릭터 결과**를 받는다.  
테스트를 끝까지 완료한 참여자는 **럭키드로우 응모 대상**이 된다.

### 1.2 핵심 경험
1. 링크 또는 QR로 이벤트 페이지 접속
2. 간단한 안내 확인
3. 객관식 질문 순차 응답
4. 응답 조합 분석
5. 한가위 캐릭터 결과 확인
6. 이름/사번/이메일 등 필요한 최소 정보 입력
7. 럭키드로우 응모 완료
8. 필요 시 결과 화면 저장/공유

### 1.3 운영 방식
- 점심시간에 주니어보드 멤버가 상주하지 않아도 참여 가능
- 모바일 중심 사용
- PC에서도 정상 동작
- 질문·선택지·캐릭터 유형·캐릭터 이미지는 **콘텐츠 데이터로 분리**
- 개발은 먼저 진행하고, 추후 질문과 캐릭터가 확정되면 데이터만 교체하여 적용
- 추첨은 이벤트 종료 후 관리자 화면 또는 데이터 내보내기를 통해 진행

---

## 2. 프로젝트 목표

### 필수 목표
- 사용자가 3~5분 내에 테스트를 완료할 수 있어야 한다.
- 질문/선택지가 확정되지 않은 상태에서도 개발이 가능해야 한다.
- 질문과 선택지 데이터가 코드에 하드코딩되지 않도록 한다.
- 캐릭터 유형과 결과 이미지 역시 나중에 쉽게 교체 가능해야 한다.
- 동일 직원의 중복 럭키드로우 응모를 방지할 수 있어야 한다.
- 테스트 결과와 럭키드로우 응모 데이터가 저장되어야 한다.
- 관리자가 참여 현황을 확인할 수 있어야 한다.

### 선택 목표
- 결과 이미지 다운로드
- 결과 공유용 카드 생성
- 캐릭터별 통계
- 질문별 선택 비율 통계
- 결과 재확인
- 이벤트 종료 후 럭키드로우 당첨자 추첨 기능

---

## 3. 비범위 / Non-goals

이번 버전에서는 아래 기능을 필수로 구현하지 않는다.

- 회원가입/로그인
- 복잡한 관리자 권한 체계
- 실시간 채팅
- 직원 간 결과 비교
- SNS 직접 공유 API
- 실물 상품 발송 기능
- 캐릭터 이미지 자동 생성 AI 기능
- 질문 자동 생성 AI 기능
- 사내 HR 시스템 연동

단, 향후 확장을 고려하여 구조를 막지는 않는다.

---

## 4. 콘텐츠 설계 원칙

### 4.1 질문 제작 순서
1. 주니어보드 멤버들이 **질문 + 객관식 선택지**를 먼저 작성
2. 질문 후보 취합
3. 최종 질문 약 8~10개 선정
4. 각 선택지에서 드러나는 행동 성향을 분석
5. 성향 축 또는 태그를 정의
6. 성향 조합에 맞춰 한가위 캐릭터 유형 설계
7. 캐릭터별 이미지 제작
8. 질문/선택지/결과 데이터를 서비스에 반영

### 4.2 질문 스타일
질문은 MBTI처럼 추상적인 자기평가 문장보다 **명절 상황에서 실제 행동을 상상하게 하는 상황형 질문**을 우선한다.

좋은 예:
- “전 부칠 사람이 부족해 보인다. 나는?”
- “오랜만에 친척들이 모두 모였다. 가장 먼저 하는 행동은?”
- “식사가 끝났는데 설거지가 산더미다. 나는?”
- “친척이 근황 질문을 시작했다. 나는?”

피해야 할 예:
- “나는 적극적인 사람이다.”
- “나는 가족들과 잘 어울린다.”
- “나는 외향적이다.”

### 4.3 선택지 작성 원칙
- 질문당 기본 4개 권장
- 정답/오답처럼 보이지 않게 작성
- 지나치게 도덕적으로 우월한 선택지가 없도록 구성
- 선택지만 보고 결과 캐릭터를 쉽게 예상하기 어렵게 구성
- 각 답변이 서로 다른 행동 성향을 드러낼 수 있어야 함
- 너무 긴 문장보다 1~2줄 내의 짧은 표현 권장

---

## 5. 예시 질문 데이터

> 아래는 구현 테스트용 샘플이다. 실제 운영 질문으로 확정된 것이 아니다.

### Q1
**전을 부치고 있는데 사람이 부족해 보인다. 나는?**

- A. 일단 앞치마부터 찾는다
- B. “뭐 도와드릴까요?” 하고 물어본다
- C. 다른 일을 하고 있는 척한다
- D. 하나 집어먹으며 상황을 지켜본다

### Q2
**식사를 마친 뒤 한두 시간이 비었다. 나는?**

- A. 가족들과 계속 이야기를 나눈다
- B. 산책이나 외출을 제안한다
- C. 소파에 누워 휴대폰을 본다
- D. 방이나 조용한 곳으로 슬쩍 이동한다

---

## 6. 결과 캐릭터 설계 방식

질문이 확정된 후 아래 두 방식 중 하나를 적용한다.

### 방식 A. 성향 점수 기반 — 권장
각 선택지에 1개 이상의 성향 점수를 부여하고, 전체 응답 점수를 합산하여 가장 가까운 캐릭터를 결정한다.

예시 성향 축:
- `HELPER` : 적극적으로 돕는 성향
- `RELAXER` : 휴식 우선 성향
- `SOCIAL` : 사람들과 어울리는 성향
- `SOLO` : 혼자 시간을 보내는 성향
- `PLANNER` : 계획·관리 성향
- `SPONTANEOUS` : 즉흥·분위기 성향
- `FOODIE` : 음식 중심 성향
- `ESCAPER` : 상황을 영리하게 빠져나가는 성향

예시:
```json
{
  "questionId": "q1",
  "optionId": "q1_a",
  "scores": {
    "HELPER": 2,
    "PLANNER": 1
  }
}
```

모든 답변을 합산한 뒤 캐릭터별 기준 벡터와 비교한다.

예:
```json
{
  "characterId": "holiday_commander",
  "targetScores": {
    "HELPER": 8,
    "PLANNER": 7,
    "SOCIAL": 5
  }
}
```

캐릭터 판정은 다음 중 하나로 구현 가능하다.

1. 가장 높은 주요 성향 2개 조합
2. 캐릭터 목표 벡터와 사용자 점수 간 거리 계산
3. 조건식 우선순위 매칭

**권장:** 질문 수가 8~10개라면 `주요 성향 2개 + 보조 성향 1개` 기반 매칭이 가장 관리하기 쉽다.

---

### 방식 B. 태그 카운트 기반 — 단순 MVP
각 선택지에 태그 1~2개를 부여한다.

예:
```json
{
  "optionId": "q1_a",
  "tags": ["HELPER", "PLANNER"]
}
```

사용자가 선택한 모든 답변의 태그를 집계한 뒤 가장 많이 나온 태그 조합으로 캐릭터를 반환한다.

장점:
- 구현이 단순함
- 운영자가 이해하기 쉬움
- 질문 추가/수정 시 관리가 쉬움

단점:
- 결과 분포가 특정 캐릭터에 치우칠 수 있음
- 정교한 캐릭터 조정이 어려움

**이번 사내 이벤트 규모에서는 태그 기반으로 먼저 구현한 후 필요 시 점수 기반으로 확장해도 충분하다.**

---

## 7. 캐릭터 데이터 구조

캐릭터 콘텐츠는 코드와 분리한다.

예시:
```json
{
  "id": "sofa_guardian",
  "name": "소파 수호자",
  "shortDescription": "명절의 진정한 목적은 휴식이라고 믿는 타입",
  "description": "해야 할 일은 있지만 일단 앉아서 상황부터 지켜봅니다. 누구보다 편안하게 연휴를 즐길 줄 아는 한가위 휴식 전문가입니다.",
  "imageUrl": "/characters/sofa_guardian.webp",
  "mainTraits": ["RELAXER", "SOLO"],
  "secondaryTraits": ["FOODIE"],
  "stats": {
    "연휴 생존력": 5,
    "눈치력": 4,
    "먹방력": 4,
    "집안일 기여도": 1
  }
}
```

### 캐릭터 결과 화면 필수 데이터
- 캐릭터명
- 캐릭터 이미지
- 한 줄 소개
- 상세 설명
- 재미 요소 2~4개
  - 능력치
  - 특징
  - 자주 하는 말
  - 잘 맞는 캐릭터
  - 주의할 캐릭터
  - 명절 추천 행동

이 중 실제 운영에 사용할 항목은 콘텐츠 확정 시 결정한다.

---

## 8. 사용자 플로우

```text
[랜딩]
   ↓
[이벤트 소개]
   ↓
[테스트 시작]
   ↓
[Q1]
   ↓
[Q2]
   ↓
 [...]
   ↓
[마지막 질문]
   ↓
[결과 계산]
   ↓
[캐릭터 결과]
   ↓
[럭키드로우 응모 정보 입력]
   ↓
[응모 완료]
   ↓
[결과 다시 보기 / 이미지 저장]
```

### 권장 UX
- 한 화면에 질문 1개
- 진행률 표시: `3 / 10`
- 선택지 클릭 시 다음 질문으로 이동
- 이전 질문으로 돌아가기 가능
- 마지막 질문 후 즉시 결과 계산
- 새로고침해도 가능하면 진행 상태 유지
- 제출 전 응답 수정 가능
- 제출 후 결과는 고정

---

## 9. 화면 구성

### 9.1 랜딩 페이지
필수 요소:
- 이벤트명
- 한 줄 소개
- 예상 소요시간
- 럭키드로우 안내
- `테스트 시작하기` 버튼

예시 문구:
> 몇 가지 질문에 답하고 나만의 한가위 캐릭터를 확인해보세요!  
> 테스트를 완료하면 한가위 럭키드로우에도 자동 응모됩니다.

---

### 9.2 질문 화면
필수 요소:
- 진행률
- 질문
- 객관식 선택지
- 이전 버튼
- 다음 처리

권장:
- 선택지를 누르면 바로 다음 질문으로 넘어가되 200~400ms 정도 선택 상태를 보여줄 것
- 모바일 터치 영역 충분히 확보
- 브라우저 뒤로가기와 앱 내 이전 버튼 충돌 주의

---

### 9.3 로딩/결과 계산 화면
선택 사항이지만 0.8~1.5초 정도 짧은 연출을 넣을 수 있다.

예:
- “당신의 한가위 캐릭터를 찾는 중…”
- “명절 행동 패턴 분석 중…”
- “복주머니 속 결과를 확인하는 중…”

실제 API 응답이 즉시 오더라도 UX 연출 목적으로 짧게 표시 가능.

---

### 9.4 캐릭터 결과 화면
필수:
- 캐릭터 이미지
- 캐릭터명
- 한 줄 설명
- 상세 설명
- 럭키드로우 응모 CTA

선택:
- 능력치
- 결과 이미지 저장
- 링크 복사
- 테스트 다시 하기

주의:
- `다시 하기`를 허용하더라도 럭키드로우 중복 응모는 방지해야 함
- 최초 결과를 저장할지, 최종 결과만 저장할지 정책 필요
- 권장: 테스트는 다시 할 수 있지만 **응모 데이터에는 최초 확정 결과만 유지**

---

### 9.5 럭키드로우 응모 화면
입력 항목은 개인정보 최소화를 원칙으로 한다.

권장 필드:
- 이름
- 사번 또는 사내 이메일 중 하나

회사 내부 식별 체계에 따라 하나만 선택.

예:
```text
이름: [          ]
사내 이메일: [                    ]
[럭키드로우 응모하기]
```

필수 동의 문구 예시:
> 입력한 정보는 이벤트 참여 확인 및 럭키드로우 추첨 목적으로만 사용되며, 이벤트 종료 후 내부 기준에 따라 삭제됩니다.

※ 실제 개인정보 처리 문구는 회사 내부 정책에 맞춰 검토 후 확정.

---

### 9.6 응모 완료 화면
표시:
- “응모가 완료되었습니다.”
- 캐릭터 결과 다시 보기
- 이벤트 종료/당첨자 발표 안내
- 필요 시 결과 이미지 저장

---

## 10. 중복 참여 정책

### 테스트
- 여러 번 가능하게 할지 운영자가 결정
- 권장: 여러 번 가능

### 럭키드로우
- 직원 1인당 1회만 응모
- 식별키 기준:
  1. 사내 이메일 권장
  2. 사번
  3. 이름+전화번호 등은 비권장

DB에 unique constraint 적용:

```sql
UNIQUE(employee_email)
```

이미 응모한 사용자가 다시 응모하면:
> 이미 럭키드로우에 참여하셨습니다.  
> 테스트 결과는 다시 확인하실 수 있어요!

와 같은 안내 노출.

---

## 11. 데이터 모델

### 11.1 `questions`
| Field | Type | 설명 |
|---|---|---|
| id | string/uuid | 질문 ID |
| order | integer | 질문 순서 |
| text | text | 질문 문구 |
| is_active | boolean | 사용 여부 |
| created_at | datetime | 생성일 |
| updated_at | datetime | 수정일 |

### 11.2 `options`
| Field | Type | 설명 |
|---|---|---|
| id | string/uuid | 선택지 ID |
| question_id | FK | 질문 ID |
| order | integer | 선택지 순서 |
| text | text | 선택지 문구 |
| score_map | json/jsonb | 성향별 점수 |
| tags | json/jsonb | 태그 배열 |
| is_active | boolean | 사용 여부 |

### 11.3 `characters`
| Field | Type | 설명 |
|---|---|---|
| id | string/uuid | 캐릭터 ID |
| name | text | 캐릭터명 |
| short_description | text | 한 줄 소개 |
| description | text | 상세 설명 |
| image_url | text | 이미지 경로 |
| traits | json/jsonb | 주요 성향 |
| stats | json/jsonb | 재미용 능력치 |
| rule | json/jsonb | 매칭 조건 |
| is_active | boolean | 사용 여부 |

### 11.4 `test_sessions`
| Field | Type | 설명 |
|---|---|---|
| id | uuid | 세션 ID |
| session_key | string | 익명 브라우저 세션 식별값 |
| started_at | datetime | 시작 |
| completed_at | datetime/null | 완료 |
| character_id | FK/null | 결과 캐릭터 |
| score_result | json/jsonb | 최종 성향 점수 |

### 11.5 `answers`
| Field | Type | 설명 |
|---|---|---|
| id | uuid | 응답 ID |
| session_id | FK | 세션 |
| question_id | FK | 질문 |
| option_id | FK | 선택지 |
| created_at | datetime | 응답 시간 |

권장 제약:
```sql
UNIQUE(session_id, question_id)
```

### 11.6 `draw_entries`
| Field | Type | 설명 |
|---|---|---|
| id | uuid | 응모 ID |
| session_id | FK | 테스트 세션 |
| employee_name | text | 이름 |
| employee_email | text | 사내 이메일 |
| character_id | FK | 응모 당시 결과 |
| entered_at | datetime | 응모 시각 |
| is_winner | boolean | 당첨 여부 |
| prize | text/null | 상품 |
| drawn_at | datetime/null | 추첨 시각 |

권장:
```sql
UNIQUE(employee_email)
```

---

## 12. 콘텐츠를 DB가 아닌 파일로 관리하는 간단한 방식

이벤트가 단발성이고 별도 CMS가 필요 없다면 MVP에서는 아래처럼 JSON/TypeScript 파일로 관리해도 된다.

```text
/content
  questions.json
  characters.json
```

`questions.json`
```json
[
  {
    "id": "q1",
    "order": 1,
    "text": "전을 부치고 있는데 사람이 부족해 보인다. 나는?",
    "options": [
      {
        "id": "q1_a",
        "text": "일단 앞치마부터 찾는다",
        "tags": ["HELPER", "PLANNER"]
      },
      {
        "id": "q1_b",
        "text": "뭐 도와드릴까요? 하고 물어본다",
        "tags": ["HELPER", "SOCIAL"]
      },
      {
        "id": "q1_c",
        "text": "다른 일을 하고 있는 척한다",
        "tags": ["ESCAPER", "SOLO"]
      },
      {
        "id": "q1_d",
        "text": "하나 집어먹으며 상황을 지켜본다",
        "tags": ["FOODIE", "RELAXER"]
      }
    ]
  }
]
```

### 권장 판단
- **질문/캐릭터를 개발자만 수정:** JSON 파일 방식
- **비개발자가 직접 수정할 필요 있음:** DB + 관리자 페이지
- **이번 이벤트만 빠르게 운영:** JSON + DB 응모 저장 방식이 가장 효율적

---

## 13. API 설계 예시

기술 스택에 따라 서버 액션이나 API Route로 대체 가능하다.

### `GET /api/questions`
활성 질문 목록 반환

Response:
```json
{
  "questions": []
}
```

### `POST /api/test/complete`
전체 답변 제출 및 캐릭터 계산

Request:
```json
{
  "sessionId": "uuid",
  "answers": [
    {
      "questionId": "q1",
      "optionId": "q1_a"
    }
  ]
}
```

Response:
```json
{
  "character": {
    "id": "sofa_guardian",
    "name": "소파 수호자"
  },
  "scores": {
    "RELAXER": 6,
    "SOLO": 4
  }
}
```

### `POST /api/draw/enter`
럭키드로우 응모

Request:
```json
{
  "sessionId": "uuid",
  "employeeName": "홍길동",
  "employeeEmail": "hong@company.com"
}
```

성공:
```json
{
  "success": true
}
```

중복:
```json
{
  "success": false,
  "code": "ALREADY_ENTERED"
}
```

### `GET /api/admin/stats`
관리자 통계

### `POST /api/admin/draw`
당첨자 추첨

---

## 14. 캐릭터 판정 로직 예시

### 14.1 태그 집계
```ts
type TraitScore = Record<string, number>;

function calculateTraits(selectedOptions): TraitScore {
  const scores: TraitScore = {};

  for (const option of selectedOptions) {
    for (const tag of option.tags) {
      scores[tag] = (scores[tag] ?? 0) + 1;
    }
  }

  return scores;
}
```

### 14.2 상위 성향 추출
```ts
function getTopTraits(scores: TraitScore, count = 3) {
  return Object.entries(scores)
    .sort((a, b) => b[1] - a[1])
    .slice(0, count)
    .map(([trait]) => trait);
}
```

### 14.3 캐릭터 매칭
각 캐릭터에 `requiredTraits`를 정의한다.

```json
{
  "id": "sofa_guardian",
  "requiredTraits": ["RELAXER", "SOLO"]
}
```

사용자의 상위 성향과 가장 많이 겹치는 캐릭터를 반환한다.

동점 시:
1. 3번째 성향 비교
2. 캐릭터 priority 비교
3. 그래도 동점이면 랜덤이 아닌 **고정 tie-breaker** 사용

같은 응답이면 항상 같은 캐릭터가 나와야 한다.

---

## 15. 결과 분포 테스트

운영 전 반드시 캐릭터 결과가 특정 유형에 과도하게 몰리지 않는지 테스트한다.

### 테스트 방법
질문 10개 × 선택지 4개인 경우 전체 조합은 매우 크므로 모든 조합을 직접 검증하지 않는다.

대신:
- 랜덤 응답 10,000~100,000회 시뮬레이션
- 캐릭터별 등장 비율 계산
- 특정 결과가 30~40% 이상이면 매칭 규칙 조정
- 거의 등장하지 않는 캐릭터도 규칙 조정

목표 예시:
- 캐릭터 8개라면 결과 비중이 대략 5~20% 범위에서 분포
- 완전히 동일한 비율일 필요는 없음

---

## 16. 럭키드로우 기능

### 16.1 기본 정책
- 테스트 완료자만 응모 가능
- 직원 1인 1응모
- 응모 기간 설정 가능
- 이벤트 종료 후 신규 응모 차단

환경 변수 또는 설정값:
```env
EVENT_START_AT=2026-09-XXT00:00:00+09:00
EVENT_END_AT=2026-09-XXT23:59:59+09:00
```

### 16.2 추첨 방식
관리자 기능에서:
1. 전체 유효 응모자 조회
2. 당첨 제외 대상 필터
3. 암호학적 난수 기반으로 무작위 선정
4. 당첨 결과 저장
5. 중복 당첨 허용 여부 설정

예:
```ts
crypto.getRandomValues(...)
```

단순 `Math.random()`보다 재현성과 신뢰성을 고려한 안전한 난수 사용 권장.

### 16.3 상품 등급 지원
선택 기능:
```json
[
  {
    "name": "황금 송편상",
    "count": 1
  },
  {
    "name": "보름달상",
    "count": 3
  },
  {
    "name": "복주머니상",
    "count": 10
  }
]
```

---

## 17. 관리자 기능

### MVP 관리자 화면
- 총 테스트 완료 수
- 총 럭키드로우 응모 수
- 캐릭터별 결과 인원
- 응모자 목록
- CSV 다운로드
- 이벤트 활성/비활성
- 럭키드로우 실행

### 선택 기능
- 질문별 선택지 응답률
- 날짜/시간별 참여 추이
- 질문/캐릭터 CRUD
- 당첨자 재추첨
- 당첨자 수동 제외
- 운영자 메모

### 보안
관리자 페이지는 반드시 일반 직원이 접근할 수 없도록 한다.

간단 구현:
- 관리자 비밀번호
- 사내 계정 로그인
- 특정 이메일 allowlist

가능하면 회사 인증 체계를 사용.

---

## 18. 개인정보 / 보안

### 최소 수집
럭키드로우 운영에 필요한 정보만 수집.

권장:
- 이름
- 사내 이메일

수집하지 않아도 되는 정보:
- 주민등록번호
- 개인 전화번호
- 주소
- 생년월일
- 비밀번호

### 필수 보안 원칙
- 관리자 API 인증
- 서버 측 중복 검증
- 사용자 입력 escape/sanitize
- DB 권한 최소화
- 운영 종료 후 응모 개인정보 삭제 정책 수립
- 클라이언트 코드에 관리자 키나 DB secret 노출 금지

---

## 19. 상태 관리

테스트 진행 중 브라우저를 닫더라도 사용성이 크게 깨지지 않도록 한다.

### 권장
`localStorage`에 임시 저장:
```json
{
  "sessionId": "...",
  "answers": {
    "q1": "q1_a",
    "q2": "q2_c"
  },
  "currentQuestion": 3
}
```

테스트 제출 후:
- 서버 저장 완료 확인
- 결과 ID 저장
- 필요 시 localStorage 정리

---

## 20. 예외 처리

### 필수 케이스

#### 질문 데이터 로드 실패
> 질문을 불러오지 못했습니다. 잠시 후 다시 시도해주세요.

`다시 시도` 버튼 제공.

#### 중간에 새로고침
이전 진행 상태 복원.

#### 모든 질문에 답하지 않고 제출
서버에서 거부:
```json
{
  "code": "INCOMPLETE_ANSWERS"
}
```

#### 중복 응모
기존 응모 내역 유지하고 친절한 메시지 표시.

#### 응모 기간 종료
> 한가위 캐릭터 테스트는 이용할 수 있지만 럭키드로우 응모 기간은 종료되었습니다.

테스트 자체를 종료할지 여부는 설정 가능하게 구현.

#### 캐릭터 매칭 실패
절대 빈 화면을 보여주지 않는다.

fallback 캐릭터 지정:
```json
{
  "fallbackCharacterId": "balanced_type"
}
```

---

## 21. 반응형 / 접근성

디자인은 별도지만 개발에서 아래 항목은 보장한다.

- Mobile First
- 360px 너비에서도 정상 작동
- 버튼 최소 터치 영역 44px 수준
- 키보드 탐색 가능
- 이미지 `alt` 제공
- 텍스트 대비 확보 가능 구조
- 질문과 선택지가 스크린리더에서 논리적 순서로 읽히도록 구현
- 애니메이션은 기능을 방해하지 않도록 함

---

## 22. 권장 기술 구성

특정 스택을 강제하지 않지만 빠른 사내 이벤트 구현 기준으로 아래 조합을 권장한다.

### Option A — 권장
- Next.js
- TypeScript
- App Router
- Tailwind CSS 또는 사용자 지정 CSS
- Supabase PostgreSQL
- Vercel

장점:
- AI 코딩 도구가 구현하기 쉬움
- 배포 간단
- DB/API/관리 기능 구현이 빠름

### Option B — 기존 사내 서버 활용
- 기존 프론트엔드 프레임워크
- 기존 백엔드
- 사내 MySQL/PostgreSQL

기존 인프라가 있다면 이를 우선한다.

### 디자인 관련
- 디자인 토큰, 컬러, 이미지, 로고는 이후 사용자 제공
- 기능 구현 단계에서는 placeholder 사용

---

## 23. 권장 디렉터리 구조 예시

```text
src/
├─ app/
│  ├─ page.tsx
│  ├─ test/
│  │  └─ page.tsx
│  ├─ result/
│  │  └─ [id]/
│  │     └─ page.tsx
│  ├─ draw/
│  │  └─ page.tsx
│  ├─ admin/
│  │  └─ page.tsx
│  └─ api/
│     ├─ test/
│     ├─ draw/
│     └─ admin/
├─ components/
│  ├─ QuestionCard.tsx
│  ├─ OptionButton.tsx
│  ├─ ProgressBar.tsx
│  ├─ CharacterResult.tsx
│  └─ DrawForm.tsx
├─ content/
│  ├─ questions.json
│  └─ characters.json
├─ lib/
│  ├─ scoring.ts
│  ├─ validation.ts
│  ├─ draw.ts
│  └─ db.ts
└─ types/
   └─ index.ts
```

---

## 24. 구현 우선순위

### Phase 1 — 오늘부터 먼저 개발
질문/캐릭터가 확정되지 않아도 구현 가능한 영역.

- 프로젝트 기본 세팅
- 랜딩 페이지 구조
- 질문 화면
- 진행률
- 선택지 선택/이전 이동
- 질문 콘텐츠 JSON 구조
- 테스트 상태 관리
- 결과 계산 인터페이스
- 임시 캐릭터 결과 화면
- 럭키드로우 입력 폼
- DB 스키마
- 응모 저장
- 중복 응모 방지
- 관리자 기본 조회 화면
- 모바일 대응

테스트 데이터는 샘플 질문과 placeholder 캐릭터로 작성.

### Phase 2 — 차주 질문 확정 후
- 실제 질문 8~10개 반영
- 각 선택지 성향 태그/점수 정의
- 캐릭터 유형 정의
- 결과 매칭 규칙 적용
- 실제 캐릭터 설명 삽입

### Phase 3 — 캐릭터 이미지 완성 후
- 실제 이미지 반영
- 디자인 적용
- 결과 카드 다듬기
- 결과 이미지 저장 기능 필요 시 추가

### Phase 4 — 오픈 전
- 결과 분포 시뮬레이션
- 모바일 실기기 테스트
- 중복 응모 테스트
- 개인정보 문구 확인
- 운영 기간 설정
- 관리자 접근 확인
- QR 코드 생성
- 실제 직원 5~10명 파일럿 테스트

---

## 25. AI 코딩 에이전트에게 전달할 구현 지침

### 핵심 지침
1. 질문과 캐릭터 콘텐츠를 컴포넌트에 하드코딩하지 말 것.
2. 질문/선택지/성향/캐릭터 데이터를 교체 가능한 구조로 만들 것.
3. 디자인은 placeholder 수준으로만 구현하고, 기능 구조에 집중할 것.
4. 모바일을 우선할 것.
5. 테스트 완료와 럭키드로우 응모를 별도 단계로 관리할 것.
6. 럭키드로우 중복 참여는 서버에서 반드시 차단할 것.
7. 같은 답변 조합은 항상 같은 캐릭터 결과를 반환할 것.
8. 캐릭터 매칭 로직은 별도 함수/모듈로 분리할 것.
9. 질문 수가 변경되어도 별도 화면 수정 없이 동작해야 할 것.
10. 질문당 선택지 개수가 달라져도 동작할 수 있게 만들 것.
11. 캐릭터 수가 늘어나거나 줄어도 코드 수정이 최소화되어야 할 것.
12. 모든 사용자 입력과 서버 요청을 validation할 것.
13. 관리자 기능은 일반 사용자에게 노출하지 말 것.
14. 에러 상태와 빈 상태를 반드시 구현할 것.
15. 이벤트 시작/종료 시각을 설정값으로 분리할 것.

---

## 26. 완료 조건 / Acceptance Criteria

### 사용자
- [ ] QR/링크로 페이지에 접속할 수 있다.
- [ ] 테스트 설명을 보고 시작할 수 있다.
- [ ] 모든 질문을 순차적으로 응답할 수 있다.
- [ ] 이전 질문으로 돌아가 답변을 수정할 수 있다.
- [ ] 현재 진행률을 알 수 있다.
- [ ] 마지막 질문 후 캐릭터 결과가 정상 계산된다.
- [ ] 결과 이미지와 설명이 표시된다.
- [ ] 테스트 완료자만 럭키드로우 응모가 가능하다.
- [ ] 동일 사내 이메일로 두 번 응모할 수 없다.
- [ ] 모바일에서 전체 플로우가 정상 작동한다.

### 관리자
- [ ] 참여자 수를 확인할 수 있다.
- [ ] 럭키드로우 응모자 수를 확인할 수 있다.
- [ ] 응모자 목록을 확인하거나 다운로드할 수 있다.
- [ ] 캐릭터별 결과 분포를 확인할 수 있다.
- [ ] 필요 시 추첨을 수행할 수 있다.
- [ ] 일반 사용자는 관리자 기능에 접근할 수 없다.

### 콘텐츠
- [ ] 질문은 데이터 파일 또는 DB만 수정하여 교체 가능하다.
- [ ] 선택지는 질문마다 개수를 다르게 설정할 수 있다.
- [ ] 캐릭터명/설명/이미지는 데이터 변경만으로 교체 가능하다.
- [ ] 캐릭터 매칭 규칙은 UI 코드와 분리되어 있다.

---

## 27. 향후 확장 아이디어

이번 이벤트에서는 필수가 아니지만 구조상 확장 가능하게 한다.

- 결과 카드 PNG 자동 생성
- 사내 메신저 공유
- “가장 많이 나온 오로라 한가위 캐릭터” 통계 공개
- 팀별 결과 분포
- 직원 전체 결과 실시간 카운터
- 캐릭터 궁합
- 캐릭터별 추천 추석 행동
- 럭키드로우 당첨 애니메이션
- 다음 사내 이벤트에서 질문/결과만 교체하여 재사용
- 범용 사내 성향 테스트 엔진으로 확장

---

## 28. 현재 확정된 운영 내용

- 이벤트 형태: **나만의 한가위 캐릭터 테스트 + 럭키드로우**
- 참여 방식: 무인 웹 참여
- 질문 형식: 상황형 객관식 중심
- 질문 수: 약 8~10개 예상
- 객관식 선택지: 질문당 약 4개 권장
- 질문/선택지부터 먼저 확정
- 질문의 답변 패턴을 분석한 후 캐릭터 유형 설계
- 캐릭터 이미지는 추후 제작 후 적용
- 테스트 완료자는 럭키드로우 응모 가능
- 개발 담당: 최호진
- 개발 시작: 즉시
- 주니어보드 멤버 질문/선택지 작성 마감: 차주 화요일
- 디자인: 별도 진행

---

## 29. 개발 시 가장 중요한 원칙

이 프로젝트의 핵심은 **“질문과 캐릭터가 아직 확정되지 않아도 개발을 먼저 끝낼 수 있는 구조”**이다.

따라서 다음 세 영역을 반드시 분리한다.

```text
1. 콘텐츠
   질문 / 선택지 / 캐릭터명 / 설명 / 이미지

2. 로직
   답변 → 성향 계산 → 캐릭터 매칭

3. UI
   질문 표시 / 선택 / 결과 표시 / 럭키드로우 응모
```

콘텐츠가 변경되더라도 로직과 UI를 거의 수정하지 않고 반영할 수 있어야 한다.

---

## 30. AI 에이전트 실행용 최종 요청문

아래 요구사항을 기준으로 웹 애플리케이션을 구현한다.

- 사내 추석 이벤트용 모바일 우선 웹페이지
- 8~10개의 객관식 상황형 질문
- 질문과 선택지는 외부 데이터(JSON 또는 DB)에서 로드
- 사용자의 답변을 기준으로 성향을 계산
- 성향 조합에 맞는 한가위 캐릭터 결과 반환
- 캐릭터 데이터 및 이미지는 추후 쉽게 교체 가능
- 테스트 완료 후 이름과 사내 이메일을 입력하여 럭키드로우 응모
- 사내 이메일 기준 1인 1응모
- 중복 검증은 반드시 서버에서 수행
- 기본 관리자 화면에서 응모자와 통계를 확인
- 디자인은 임시 placeholder 수준으로 구현
- 추후 디자인 교체를 고려하여 컴포넌트를 분리
- TypeScript 사용 권장
- 로직과 UI를 분리
- 테스트 가능한 순수 함수 형태로 캐릭터 계산 로직 작성
- 개인정보와 관리자 인증을 고려
- 질문 수, 선택지 수, 캐릭터 수가 변경되어도 정상 작동하는 데이터 중심 구조로 구현
